Subir:
Nueva informacion
Estar en la rama adecuada
git add -A
git commit -m "" (guardar)
git push
realizar pull request

Traer:
git fetch (trae todas las ramas)
pararse en main local y pull
pararse en rama y realizar merge desde main local a la rama
git pull (Rama especifica)
git stash (no guardar)

Orden de excepciones:
Repositorio-Servicio_controlador


// Logger logger = LoggerFactory.getLogger(AccountRepository.class);